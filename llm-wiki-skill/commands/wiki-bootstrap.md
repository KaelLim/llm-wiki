# Wiki Bootstrap

Use the `superpowers:llm-wiki` skill to set up a new LLM Wiki knowledge base in the current project.

Run this when starting a fresh wiki. It creates the directory structure, initial index, and CLAUDE.md schema.
